import { ProductContent } from "./ProductContent";

export const metadata = {
    title: "Product Capabilities | Surveilens",
    description: "See how Surveilens transforms your existing camera fleet into an intelligent threat detection system.",
};

export default function ProductPage() {
    return <ProductContent />;
}
